export function getUserData(state) {
    return state.userData;
  }

export function getDataError(state) {
    return state.dataError;
}

export function getReposData(state) {
    return state.reposData;
}

